import 'package:flutter/material.dart';

class PilotOffer extends StatelessWidget {
  const PilotOffer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}