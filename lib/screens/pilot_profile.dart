import 'package:flutter/material.dart';

class PilotProfile extends StatelessWidget {
  const PilotProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}