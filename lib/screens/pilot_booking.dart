import 'package:flutter/material.dart';

class PilotBooking extends StatelessWidget {
  const PilotBooking({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}